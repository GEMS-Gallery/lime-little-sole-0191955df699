service : {
  getPlayersState: () -> (vec record {
                                nat;
                                opt text;
                                nat;
                                nat;
                                vec nat;
                              }) query;
  resetCounters: () -> ();
  setPlayerName: (nat, opt text) -> ();
  updateCommanderDamage: (nat, nat, int) -> ();
  updateLifeTotal: (nat, int) -> ();
  updatePoisonCounters: (nat, int) -> ();
}
